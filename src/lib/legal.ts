// Informations légales affichées sur les pages « Mentions légales » et « Confidentialité ».
// Une valeur à `null` s'affiche « à compléter » sur le site : remplis-la ici avec les vraies données.
export const LEGAL = {
  /** Nom de l'association qui édite le site. */
  name: null as string | null,
  /** Forme juridique, par exemple « Association loi 1901 ». */
  form: null as string | null,
  /** Adresse postale du siège. */
  address: null as string | null,
  /** Adresse e-mail de contact, aussi utilisée pour exercer les droits RGPD. */
  email: null as string | null,
  /** Nom du directeur ou de la directrice de la publication. */
  director: null as string | null,
  /** Hébergeur du site (nom, adresse) : par exemple celui du déploiement Next.js. */
  host: null as string | null,
  /** Région où la base de données Supabase est hébergée, par exemple « Union européenne (Francfort) ». */
  databaseRegion: null as string | null,
  /** Date de dernière mise à jour des pages légales. */
  updatedAt: "22 septembre 2026",
};
